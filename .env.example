# Required: Discord webhook URL for the channel where you want news notifications.
# Create one in Discord: Server → Channel → Edit Channel → Integrations → Webhooks → New Webhook.
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# Optional: TradingEconomics API key. If unset, uses guest:guest (limited access; may have stricter rate limits).
# Get a key at https://tradingeconomics.com/api/
# TRADING_ECONOMICS_API_KEY=your_key_here

# Optional: When using --loop, interval in seconds between runs (default: 900 = 15 min).
# NOTIFIER_INTERVAL_SECONDS=900
